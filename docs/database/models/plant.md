# Plant

## Overview

Represents a plant owned and managed by a user.  
Stores user‑specific information such as the plant’s nickname, species, watering schedule and notes.

## Fields

| Field                     | Type    | Optional | Description                                           |
| ------------------------- | ------- | -------- | ----------------------------------------------------- |
| id                        | string  | no       | Unique identifier generated by Firebase               |
| name                      | string  | no       | Name or nickname given by the user                    |
| species                   | string  | yes      | User‑entered species name (not linked to Species yet) |
| creationDate              | Date    | no       | Timestamp when the plant was created                  |
| lastWateredDate           | Date    | yes      | Timestamp of the most recent watering                 |
| secondLastWateredDate     | Date    | yes      | Timestamp of the second most recent watering          |
| wateringFrequency         | number  | yes      | Days between watering events                          |
| inferredWateringFrequency | number  | yes      | Derived watering interval from watering history       |
| nextWateringDate          | Date    | yes      | Next scheduled watering date                          |
| trackWatering             | boolean | yes      | Whether to show watering schedule in the dashboard    |
| imageUrl                  | string  | yes      | Stored URL for the plant image                        |
| notes                     | string  | yes      | Free‑form notes added by the user                     |

History is stored as a subcollection and documented separately.

## Relationships

- (future) **Plant → Species** (many‑to‑one)  
  A plant references a single species entry, but many plants may reference the same species.

- **Plant → History** (one‑to‑many)  
  A plant can have multiple history events (watering, fertilizing, repotting, etc.).

- **Plant → User** (many‑to‑one)  
  Each plant belongs to a single user, but a user can have many plants.

## Firestore Storage

`/users/{userId}/plants/{plantId}`

History subcollection:
`/users/{userId}/plants/{plantId}/careEntries/{eventId}`

## Constraints and Rules

- `name` must be a non‑empty string
- `creationDate` must always be set
- `wateringFrequency` must be a positive number if provided
- `lastWateredDate` may be null if the plant has never been watered
- `secondLastWateredDate` may be null if there are fewer than two watering events
- `inferredWateringFrequency` may be 0 when history is insufficient
- `nextWateringDate` may be null when scheduling cannot be derived
- `trackWatering` is typically `true` when watering data is available, but may be `false` if the user opts out of tracking
- `notes` is optional and unstructured
- `species` is an optional string
- `imageUrl` is optional and may be empty

## Example Document

```json
{
  "id": "abc123",
  "name": "Monstera",
  "species": "Monstera Deliciosa",
  "creationDate": "2026-01-12T10:30:00Z",
  "lastWateredDate": "2026-01-20T09:00:00Z",
  "secondLastWateredDate": "2026-01-12T09:00:00Z",
  "wateringFrequency": 7,
  "inferredWateringFrequency": 8,
  "trackWatering": true,
  "nextWateringDate": "2026-01-28T09:00:00Z",
  "imageUrl": "https://example.com/plants/monstera.jpg",
  "notes": "Repotted in june 2025"
}
```
